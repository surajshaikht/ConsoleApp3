on: [push]

jobs:
  build:
  name: Hello World
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v2
  - name: Write a multi-line message
    run:  |
      echo This is demo file show a
      echo Very basic and easy to understand workflow.
